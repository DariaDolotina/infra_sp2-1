FROM nginx:1.19.0-alpine
# Не хватило информации: как вмонтировать в докер-композ.
# В теории курса ноль инфы, то, что нагуглил всегда вело к ошибке "нет такой директории".
# Чтобы устранить замечание, мне нужно либо больше инфы
COPY ./default.conf /etc/nginx/conf.d/default.conf
